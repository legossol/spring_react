server:
  port: 8080
spring:
  datasource:
    url: jdbc:mysql://legossol.crrrqqjyomfc.ap-northeast-2.rds.amazonaws.com
    username: legossol
    password: legossol
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    database-platform: org.hibernate.dialect.MySQL5InnoDBDialect
    open-in-view: false
    generate-ddl: true
    show-sql: true
    hibernate:
      ddl-auto: update
  jwt:
    secret: jwtSecret
UserController:
  signup: Creates user and returns its JWT token
  signin: Authenticates user and returns its JWT token
